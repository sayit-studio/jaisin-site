# 宅心驗屋官網 — 更新紀錄

> repo：`sayit-studio/jaisin-site`（原始碼）｜預覽站：GitHub Pages `/jaisin-site/`
> 正式站 `sayit-studio/jaisin` 為獨立部署 repo，本次更新尚未同步過去。

---

## 2026-08-12 ~ 08-13｜內容校正與版型統一

依屋主（業主）逐頁截圖標註進行的文案校正、服務範圍界定與全站視覺統一。

### 一、專業用詞與服務範圍校正

| 項目 | 內容 | Commit |
|---|---|---|
| 泄水坡度 | 全站「排水坡度／洩水坡度」統一為**泄水坡度**（含 SEO、FAQ、案例標籤、設計系統與文件） | `a983192` |
| 不做修繕語感 | 案例移除「拆開後確認」，改為記錄存證；「倒水測試」改為**灑水測試** | `a983192` |
| 不驗外牆 | 全站「外牆」改為**牆面**；透天 FAQ「頂樓和外牆也會一起檢查嗎」整題刪除 | `8813b69` `723b8ee` |
| 不驗結構 | 移除「結構檢視／結構安全」宣稱，新增 FAQ 明說**驗屋不含結構鑑定** | `8813b69` |
| 空鼓與熱顯像分離 | 牆面空心與裂痕改為逐面敲擊；熱顯像案例只講滲水與含水率 | `8813b69` |
| 頂樓檢測 | 案例改為**放水靜置＋下層天花板熱顯像與含水率** | `8813b69` |
| 中古屋進場時機 | 看屋當下無法驗；改為「交屋前經賣方同意」或「交屋後由新屋主安排」，移除全站「議價前驗屋」說法 | `8813b69` |
| 天花板接縫 | 副標移除「輕隔間」，改為接縫開裂、批土脫落與板材錯位 | `a983192` |
| 六大重點調整 | 透天移除「通風與採光死角」，改為**衛浴與廚房設備**；「樓梯與結構安全」改為「樓梯與扶手」 | `8813b69` |

### 二、FAQ 事實校正

| 項目 | 修正後 | Commit |
|---|---|---|
| 預約資料 | 改要**建商提供的驗屋日期區間**（移除「預計交屋日期」） | `973e546` |
| 預約時機 | 建議**驗屋日前 1~2 個月**聯繫（原為點交前 1~2 週） | `973e546` |
| 臨時預約 | 明說排程依建商驗屋區間、**無法臨時預約** | `973e546` |
| 檢測項目 | 補上灑水測試、地排內視鏡、衛浴廚房設備五金 | `973e546` |
| 社區團購 | 「聯繫宅心官方 LINE 立刻幫您開團」 | `973e546` |
| 報告時效 | **驗屋日隔天起算五個工作天**（原為 1~3 個工作日），首頁 FAQ 同步 | `973e546` |
| 交涉協助 | 驗屋當天**驗屋師主管帶屋主與建商核對缺失**並協助溝通修繕標準 | `b903e13` |
| 驗屋流程 | 04/05 改為「當天解說 → 完整報告」，全站移除「報告說明」階段 | `a983192` |

### 三、服務與版型調整

- **移除裝潢細清**：分頁、路由、預渲染路徑、導覽選單、首頁卡片、meta／OG／JSON-LD 與 FAQ 字樣全數移除；圖片素材保留備用，`public/Home-Inspection.html`（舊版靜態頁）未更動。全屋整裝保留。（`5cddee5`）
- **全站小字移除**：Banner 黃色 eyebrow 與 19 處區塊小標籤全部拿掉，`BannerHero` 不再接受 `eyebrow`。（`d39de6b`）
- **關於頁專業團隊隱藏**：以 `SHOW_TEAM = false` 旗標關閉，改 `true` 即可恢復。（`d39de6b`）
- **分頁視覺統一**（以新成屋頁為基準）：`PageHeader` 改為沿用 `BannerHero`；卡片圓角統一 `--radius-md`；區塊背景改 base／subtle 交替（`bg-warm` 汰除）。（`f76be57`）
- **說明會頁**：Hero 改用 `BannerHero`（新增 `action` 插槽保留「查看近期場次」按鈕），並補上實際 banner 圖，移除殘留的 `.seminar-hero` 樣式。（`aee9877`）
- **合作洽詢 CTA 修正**：深藍底標題因 `global.css` 的 `h1–h6` 直接指定字色而變成深藍字，改為明確指定 `--color-text-inverse`。（`924ea9e`）

### 四、服務足跡區塊改版（`095e3ac`）

原本每張社區卡片都頂著空的灰色圖框，沒有照片時反而顯得單薄。改為不依賴照片的三段式呈現：

- 自繪低多邊形**台灣輪廓 SVG**（非地圖資料、零版權疑慮），標出服務縣市，點擊／hover／鍵盤切換該縣市建案清單
- **數字牆**沿用關於頁既有數據（130+ 服務社區、50+ 品牌建商、8+ 服務縣市），未新增未經查證的數字
- 建案名改為**雙排反向捲動的文字 chip**，密度為原本圖卡的兩倍

### 五、素材

- 新增 `assets/banners/seminar-desktop.webp`（1920×900）、`seminar-mobile.webp`（900×1200）—— AI 生成的說明會現場情境照，取代文字佔位圖 `hero-seminar.jpg`（保留備用）。

---

## 待辦（下次繼續）

| # | 項目 | 說明 |
|---|---|---|
| 1 | **新成屋案例 02 圖片** | `assets/cases/new-home-02.webp` 仍是空房間＋窗戶照，與「浴室泄水坡度」文案不符。規格與 AI Prompt 見 `../docs/PLACEHOLDER_IMAGES.md`，同檔名覆蓋即可，不需改程式碼 |
| 2 | **服務足跡建案清單** | 目前僅 12 筆示意資料（`src/components/ui/LogoLoop.jsx` 的 `FOOTPRINT`）。實際 130+ 清單整理好即可補入，地圖縣市點會自動增加 |
| 3 | **建案實拍照** | 決議不從 Google 抓圖（版權風險）。改為向建商索取授權照，或驗屋現場自行拍攝累積圖庫；取得後可掛進 `FOOTPRINT` 的縣市卡片，版面不需重做 |
| 4 | **Banner 文案換行** | 關於／常見問題／社區團購／合作洽詢原本設定為標題強制單行，統一後改為自然換行；若要恢復單行需另外處理 |
| 5 | **正式站同步** | 本次全部只推到預覽 repo，正式站 `sayit-studio/jaisin` 尚未部署 |
| 6 | **既有 lint 警告** | `Navbar.jsx` 的 `useEffect` 內同步 setState（既有問題，與本次改動無關） |
